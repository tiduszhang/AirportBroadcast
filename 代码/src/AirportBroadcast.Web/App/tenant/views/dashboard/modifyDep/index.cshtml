<div class="modal-header">
    <h4 class="modal-title">
        <span>出港</span> 
    </h4>
</div>
<div class="modal-body">
    <div class="row">
        <div class="col-md-3"  style="text-align:center;">
            <div class="radio-list">                 
                <label>
                    <input type="radio" name="gender" value="DLY_C" ng-model="vm.paramters.playCommand" /> 航班延误通知
                </label>
                <label>
                    <input type="radio" name="gender" value="CAN_C" ng-model="vm.paramters.playCommand" /> 航班取消通知
                </label>
            </div> 
        </div>
        <div class="col-md-9">             
            <div class="form-group" ng-if="vm.paramters.playCommand=='DLY_C'">
                <label class="control-label">预计起飞时间</label>
                <input type="time" class="form-control" ng-model="vm.chooseTime">
            </div>

            <div class="form-group" ng-if="vm.paramters.playCommand=='DLY_C' || vm.paramters.playCommand=='CAN_C'">
                <label class="control-label">{{vm.paramters.playCommand=='DLY_C'?"延误原因":"取消原因"  }}</label>
                <select class="form-control edited" multiple="multiple" size="5" ng-model="vm.chooseReasions">
                    <option ng-repeat="item in vm.reasions" value="{{item.code}}">
                        <text class="checkbox"> {{item.content}}</text>
                    </option>
                </select>
            </div>
        </div>
    </div>
</div>
<div class="modal-footer">
    <button ng-disabled="vm.loading" type="button" class="btn btn-default" ng-click="vm.cancel()">取消</button>
    <button type="submit" button-busy="vm.loading" busy-text="加载中" class="btn btn-primary blue" ng-click="vm.play()" ng-disabled="createOrEditForm.$invalid"><i class="fa fa-save"></i> <span>播放</span></button>
</div>